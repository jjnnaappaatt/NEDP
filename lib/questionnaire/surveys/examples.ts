/** Starter templates shown in the admin/integrate "load example" dropdown, as examples of the
 *  import format — not pre-seeded questionnaires. The original 5 examples were verbatim real
 *  project questionnaires and have been removed for this public repo; drop your own JSON files
 *  here (see any `schema.ts` type for the shape) and list them below to restore the feature. */
export const SURVEY_EXAMPLES: { key: string; label: string; raw: unknown }[] = [];
